<%- include("partials/header") %>

<% blogs.forEach(function(blog){ %>
    <div>
        <h1><%=blog.title%></h1>
        <img style="width: 800px; height: 400px" src="<%=blog.image%>" alt="">
        <span><%=blog.created%></span>
        <p><%=blog.body%></p>
    </div>
<% }) %>

<%- include("partials/footer") %>